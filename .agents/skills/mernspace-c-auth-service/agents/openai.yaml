interface:
  display_name: "Mernspace C Auth Service"
  short_description: "Repo-specific patterns and workflows for mernspace-c-auth-service"
  default_prompt: "Use the mernspace-c-auth-service repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true